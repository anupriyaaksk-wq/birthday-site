<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <title>Happy Birthday!</title>
  <style>
    body {
      margin: 0;
      height: 100vh;
      background: linear-gradient(270deg, #fbc2eb, #a6c1ee, #ffdde1);
      background-size: 600% 600%;
      animation: backgroundShift 12s ease infinite;
      font-family: 'Comic Sans MS', cursive, sans-serif;
      text-align: center;
      overflow: hidden;
      position: relative;
    }

    @keyframes backgroundShift {
      0% { background-position: 0% 50%; }
      50% { background-position: 100% 50%; }
      100% { background-position: 0% 50%; }
    }

    h1 {
      font-size: 3em;
      color: #ff1493;
      text-shadow: 2px 2px #fff;
      margin-top: 30px;
      animation: bounceIn 2s ease;
    }

    p {
      font-size: 1.5em;
      color: #222;
      margin: 15px 0;
      animation: fadeIn 3s ease;
    }

    @keyframes bounceIn {
      0% { transform: scale(0.3); opacity: 0; }
      50% { transform: scale(1.1); opacity: 1; }
      70% { transform: scale(0.9); }
      100% { transform: scale(1); }
    }

    @keyframes fadeIn {
      from { opacity: 0; }
      to { opacity: 1; }
    }

    .photos {
      display: flex;
      justify-content: center;
      flex-wrap: wrap;
      gap: 20px;
      margin-top: 40px;
      z-index: 2;
      position: relative;
    }

    .photos img {
      width: 200px;
      height: 200px;
      object-fit: cover;
      border-radius: 20px;
      border: 4px solid white;
      box-shadow: 0 8px 20px rgba(0,0,0,0.4);
      transition: transform 0.4s ease, box-shadow 0.4s;
    }

    .photos img:hover {
      transform: scale(1.1) rotate(2deg);
      box-shadow: 0 12px 30px rgba(255, 20, 147, 0.6);
    }

    /* 🎈 Floating Balloons */
    .balloon {
      position: absolute;
      bottom: -150px;
      font-size: 2.5em;
      animation: floatUp 10s linear infinite;
    }

    @keyframes floatUp {
      0% { transform: translateY(0); opacity: 1; }
      100% { transform: translateY(-120vh); opacity: 0; }
    }

    /* 🎊 Confetti */
    .confetti {
      position: absolute;
      width: 10px;
      height: 10px;
      background: red;
      top: -10px;
      animation: fall 5s linear infinite;
    }

    @keyframes fall {
      to {
        transform: translateY(110vh) rotate(360deg);
      }
    }
  </style>
</head>
<body>
  <h1>🎉 Happy Birthday, [Name]! 🎂</h1>
  <p>Wishing you a day filled with love, laughter, and unforgettable memories! 💖</p>

  <div class="photos">
    <!-- Replace these with real birthday person's pictures -->
    <img src="i2.jpeg" alt="Beautiful Sakshi">
    <img src="i1.jpeg" alt="Cute Sakshi">
    <img src="i3.jpeg" alt="Hot sakshi">
  </div>

  <!-- 🎈 Floating Balloons -->
  <div class="balloon" style="left: 10%;">🎈</div>
  <div class="balloon" style="left: 30%; animation-delay: 2s;">🎈</div>
  <div class="balloon" style="left: 50%; animation-delay: 4s;">🎈</div>
  <div class="balloon" style="left: 70%; animation-delay: 6s;">🎈</div>
  <div class="balloon" style="left: 90%; animation-delay: 8s;">🎈</div>

  <!-- 🎊 Confetti (randomized with JS) -->
  <script>
    function createConfetti() {
      const confetti = document.createElement("div");
      confetti.classList.add("confetti");
      document.body.appendChild(confetti);

      const size = Math.random() * 8 + 5 + "px";
      confetti.style.width = size;
      confetti.style.height = size;

      confetti.style.left = Math.random() * window.innerWidth + "px";

      const colors = ["#ff0", "#ff1493", "#00f", "#0f0", "#f90", "#0ff"];
      confetti.style.backgroundColor = colors[Math.floor(Math.random() * colors.length)];

      confetti.style.animationDuration = (Math.random() * 3 + 2) + "s";

      setTimeout(() => {
        confetti.remove();
      }, 5000);
    }

    setInterval(createConfetti, 200);
  </script>
</body>
</html>

